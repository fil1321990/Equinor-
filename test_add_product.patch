--- src/App.tsx
+++ src/App.tsx
@@ -7231,2 +7231,18 @@
                 </div>
+                
+                {/* Preview block */}
+                {(newProductRoi || newProductFixedDaily) && newProductMin && newProductDays && (
+                  <div className="bg-indigo-50 border border-indigo-100 p-4 rounded-xl">
+                    <p className="text-xs font-bold text-indigo-800 mb-2 uppercase tracking-wider">Preview Income</p>
+                    <div className="grid grid-cols-2 gap-4">
+                      <div>
+                        <p className="text-xs text-indigo-600">Daily Income</p>
+                        <p className="text-sm font-bold text-indigo-900">₦{newProductFixedDaily ? Number(newProductFixedDaily).toLocaleString() : (Number(newProductMin) * (1 + Number(newProductRoi) / 100) / Number(newProductDays)).toLocaleString(undefined, {minimumFractionDigits:2, maximumFractionDigits:2})}</p>
+                      </div>
+                      <div>
+                        <p className="text-xs text-indigo-600">Total Return</p>
+                        <p className="text-sm font-bold text-indigo-900">₦{newProductFixedDaily ? (Number(newProductFixedDaily) * Number(newProductDays)).toLocaleString() : (Number(newProductMin) * (1 + Number(newProductRoi) / 100)).toLocaleString()}</p>
+                      </div>
+                    </div>
+                  </div>
+                )}
